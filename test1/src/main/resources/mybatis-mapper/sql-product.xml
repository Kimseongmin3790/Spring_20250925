<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.ProductMapper">
	<select id="selectFoodList" parameterType="hashmap" resultType="com.example.test1.model.Product">
		SELECT *
		FROM TBL_FOOD F
		INNER JOIN TBL_FOOD_IMG I ON F.FOOD_NO = I.FOOD_NO
		WHERE 1=1 
		<if test="keyword != ''">
			AND FOOD_NAME LIKE '%${keyword}%' OR FOOD_INFO LIKE '%${keyword}%'
		</if>
		<if test="menuNo != null and menuNo != ''">
			AND F.MENU_NO = #{menuNo}
		</if>
		<if test="menuPart != null and menuPart != ''">
			AND F.MENU_PART = #{menuPart}
		</if>
	</select>
	
	<select id="selectMenuList" parameterType="hashmap" resultType="com.example.test1.model.Menu">
		SELECT M.*, NVL(CNT, 0) CNT
		FROM TBL_MENU M
		LEFT JOIN (
		    SELECT M1.MENU_NO, COUNT(*) CNT
		    FROM TBL_MENU M1
		    INNER JOIN TBL_MENU M2 ON M1.MENU_NO = M2.MENU_PART
		    GROUP BY M1.MENU_NO
		) T ON M.MENU_NO = T.MENU_NO
		ORDER BY M.MENU_NO
	</select>
	
	<select id="selectFoodKind" parameterType="hashmap" resultType="com.example.test1.model.Menu">
		SELECT * 
		FROM TBL_MENU
		WHERE DEPTH = 1
	</select>
	
	<insert id="insertFood" parameterType="hashmap">
		INSERT INTO TBL_FOOD
		VALUES(FOOD_SEQ.NEXTVAL, #{foodName}, #{price}, #{foodKind}, #{foodInfo}, 'Y', null, #{menuPart})
	</insert>
</mapper>




