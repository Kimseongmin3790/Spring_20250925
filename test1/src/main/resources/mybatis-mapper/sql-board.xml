<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="boardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT B.BOARDNO, TITLE, B.USERID, CNT, COUNT(COMMENTNO) AS COUNT, DECODE(TO_CHAR(B.CDATETIME, 'YYYY-MM-DD'), TO_CHAR(SYSDATE, 'YYYY-MM-DD'), TO_CHAR(B.CDATETIME, 'HH24:MI:SS'), TO_CHAR(B.CDATETIME, 'YYYY-MM-DD')) AS CDATE
		FROM TBL_BOARD B
		LEFT JOIN TBL_COMMENT C ON B.BOARDNO = C.BOARDNO
		WHERE 1=1 
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		<if test="searchKind == ''">
			AND 
			(TITLE LIKE '%' || #{keyword} || '%' OR B.USERID LIKE '%' || #{keyword} || '%')
		</if>
		<if test="searchKind == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchKind == 'userId'">
			AND B.USERID LIKE '%' || #{keyword} || '%'
		</if>	
		GROUP BY B.BOARDNO, TITLE, B.USERID, CNT, B.CDATETIME
		<if test="order == 'no'">
			ORDER BY BOARDNO ASC
		</if>
		<if test="order == 'title'">
			ORDER BY TITLE ASC
		</if>
		<if test="order == 'cnt'">
			ORDER BY CNT DESC
		</if>
		<if test="order == 'time'">
			ORDER BY B.CDATETIME DESC
		</if>
		OFFSET #{page} ROWS FETCH NEXT #{pageSize} ROWS ONLY
	</select>
	
	<select id="selectBoardCnt" parameterType="hashmap" resultType="int">
		SELECT COUNT(*)
		FROM TBL_BOARD B
		WHERE 1=1 
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		<if test="searchKind == ''">
			AND 
			(TITLE LIKE '%' || #{keyword} || '%' OR B.USERID LIKE '%' || #{keyword} || '%')
		</if>
		<if test="searchKind == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchKind == 'userId'">
			AND B.USERID LIKE '%' || #{keyword} || '%'
		</if>
		
	</select>
	
	<delete id="boardRemove" parameterType="hashmap">
		DELETE FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</delete>
	
	<insert id="boardInsert" parameterType="hashmap" useGeneratedKeys="true" keyColumn="BOARDNO" keyProperty="boardNo">
		INSERT INTO TBL_BOARD
		VALUES(B_SEQ.NEXTVAL, #{title}, #{contents}, #{userId}, 0, 0, #{kind}, SYSDATE, SYSDATE)
	</insert>
	
	<select id="boardInfo" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *
		FROM TBL_BOARD
		WHERE BOARDNO = #{boardNo}
	</select>
	
	<update id="boardEdit" parameterType="hashmap">
		UPDATE TBL_BOARD
		SET TITLE = #{title}, USERID = #{userId}, CONTENTS = #{contents}
		WHERE BOARDNO = #{boardNo}
	</update>
	
	<select id="boardComment" parameterType="hashmap" resultType="com.example.test1.model.Comment">
		SELECT C.*, U.NICKNAME
		FROM TBL_COMMENT C
		INNER JOIN TBL_USER U ON C.USERID = U.USERID
		WHERE BOARDNO = #{boardNo}
		ORDER BY C.CDATETIME ASC
	</select>
	
	<insert id="commentInsert" parameterType="hashmap">
		INSERT INTO TBL_COMMENT
		VALUES(COMMENT_SEQ.NEXTVAL, #{boardNo}, #{contents}, #{userId}, 0, NULL, SYSDATE, SYSDATE)
	</insert>
	
	<delete id="commentDelete" parameterType="hashmap">
		DELETE FROM TBL_COMMENT
		WHERE COMMENTNO = #{commentNo}
	</delete>
	
	<update id="boardCnt" parameterType="hashmap">
		UPDATE TBL_BOARD SET CNT = CNT + 1 WHERE BOARDNO = #{boardNo}
	</update>
	
	<delete id="deleteBoardList" parameterType="hashmap">
		DELETE FROM TBL_BOARD
		WHERE BOARDNO IN
		<foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
			#{item}
		</foreach>
	</delete>
	
	<insert id="insertBoardImg" parameterType="hashmap">
		INSERT INTO TBL_BOARD_FILE
		VALUES(FILE_SEQ.NEXTVAL, #{boardNo}, #{path}, #{fileName}, #{orgName}, #{size}, #{ext})
	</insert>
	
	<select id="selectFileList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *
		FROM TBL_BOARD_FILE
		WHERE BOARDNO = #{boardNo}
	</select>
	
</mapper>




